CREATE TABLE recibos (motivo VARCHAR(255) NULL, estadotransaccion VARCHAR(255) NULL, fechaconfeccion DATETIME NULL, razonsocial VARCHAR(255) NULL, numerocuota VARCHAR(255) NULL, estadorecibo VARCHAR(255) NULL, numeroacta INTEGER NULL, monto FLOAT(32) NULL, banco VARCHAR(255) NULL, fechavencimiento DATETIME NULL, numerocheque VARCHAR(255) NULL, usuario VARCHAR(255) NOT NULL, numero INTEGER NOT NULL, PRIMARY KEY (usuario, numero))
CREATE TABLE usuarios (usuario VARCHAR(255) NOT NULL, sede VARCHAR(255) NULL, sector VARCHAR(255) NULL, tipocuenta VARCHAR(255) NULL, digitos VARCHAR(255) NULL, password VARCHAR(255) NULL, habilitado INTEGER NULL, responsable VARCHAR(255) NULL, PRIMARY KEY (usuario))
ALTER TABLE recibos ADD CONSTRAINT FK_recibos_usuario FOREIGN KEY (usuario) REFERENCES usuarios (usuario)
